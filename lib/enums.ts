export enum Role {
    USER = 'USER',
    ADMIN = 'ADMIN',
}

export enum MessageType {
    TEXT = 'TEXT',
    IMAGE = 'IMAGE',
    FILE = 'FILE',
    VIDEO = 'VIDEO',
    NOTIFICATION = 'NOTIFICATION',
}

export enum ConversationRole {
    MEMBER = 'MEMBER',
    ADMIN = 'ADMIN',
}
export enum MovieType {
    MOVIE = 'MOVIE',
    TV = 'TV',
}
